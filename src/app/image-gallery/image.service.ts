import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class ImageService {
    // private apiUrl ='https://localhost:7261/swagger/index.html';

  // constructor(private http: HttpClient) { }

//   saveImage(image: File): Observable<any> {
//     const formData = new FormData();
//     formData.append('image', image);
//     return this.http.post('your-api-endpoint', formData);
//   }

// saveImage(imageData: string) {
    // return this.http.post(this.apiUrl + '/saveimage', { imageData });
  }
// }
